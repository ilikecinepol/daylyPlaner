services:
  - type: web
    name: daylyplaner
    runtime: docker
    repo: https://github.com/ilikecinepol/daylyPlaner
    branch: master
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port $PORT
    preDeployCommand: alembic upgrade head
    healthCheckPath: /api/v1/health
    autoDeployTrigger: commit
    envVars:
      - key: APP_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: COOKIE_SECURE
        value: "1"
      - key: DATABASE_URL
        fromDatabase:
          name: daylyplaner-db
          property: connectionString

databases:
  - name: daylyplaner-db
    plan: free
    region: frankfurt
    postgresMajorVersion: "16"
    databaseName: daylyplaner
    user: daylyplaner
    ipAllowList: []
